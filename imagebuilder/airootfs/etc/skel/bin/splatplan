#!/usr/bin/bash
if [[ -f /usr/bin/splatpost ]]; then
    echo "You're trying to run splatpost without root privilege. Please run:"
    echo sudo splatpost $@
else
    echo "The splatplost is not installed. Please run:"
    echo "install_splatplost"
fi
